Ок — ниже готовый, простой и быстрый вариант InMemory TTL cache + TiledImageService, который:

создаётся с инжектом repo + cache

при get_tile() сначала смотрит в кеш по ключу (uuid, z, y, x, fmt)

если нет/протух — читает из хранилища, кладёт в кеш с TTL

умеет базовые лимиты, чтобы кеш не “съел” RAM: max_items и max_bytes

(опционально) кеширует ещё и manifest

---

Почему мы возвращаем bytes, а не StreamingResponse напрямую?
Потому что кешировать стрим неудобно. Тайл обычно небольшой (256/512 webp/png), его нормально держать как bytes.

---

Если get_tile_repo возвращает конкретно S3TileRepository / FileSystemTileRepository, всё ок — оба имеют open_tile и get_manifest.

---

4) Меняем endpoint GET /tiles/{uuid}/{z}/{y}/{x} чтобы ходил через сервис
Теперь логика такая:

endpoint → TiledImageService.get_tile_bytes() → кеш → репо → кеш.
---

6) Важно: инвалидировать кеш при удалениях (желательно)

Сейчас у тебя есть delete endpoints. Минимальный вариант — при удалении:

удалить ключ конкретного тайла из кеша

при bulk delete — удалить manifest из кеша и (по желанию) cache.clear() или выборочно (но выборочно нужен индекс ключей)

Самый простой практичный вариант:

при DELETE /tiles/{uuid} → cache.delete(("manifest", uuid)) и (если не хочешь усложнять) cache.clear()
(или оставить как есть — через TTL всё само уйдёт, но будет короткий период, где кеш отдаст старое)

---

(Полное clear() — самый простой вариант, но “снесёт” кеш всех изображений. Если хочешь — сделаю аккуратно: хранить индекс ключей по uuid и чистить только его.)